# ✈️ Nabi's Travel Plans

我的个人旅行计划网站，使用 React + TypeScript + Vite 构建，部署在 GitHub Pages。

## 🌐 访问地址

- 主页：https://nabiwu.github.io/nabi-trip/
- 墨西哥行程：https://nabiwu.github.io/nabi-trip/trips/mexico
- 行程状态：https://nabiwu.github.io/nabi-trip/trips/mexico/status

## 🎨 技术栈

- **React 18** - 现代化 UI 框架
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具
- **React Router** - 客户端路由
- **Tailwind CSS** - 实用优先的 CSS 框架
- **GitHub Pages** - 静态网站托管

## 📁 项目结构

```
nabi-trip/
├── src/
│   ├── components/      # React 组件
│   │   ├── TodayButton.tsx    # 智能"今天"按钮
│   │   ├── TripCard.tsx        # 旅行卡片
│   │   └── DayCard.tsx         # 日期卡片
│   ├── pages/           # 页面组件
│   │   ├── Home.tsx            # 主页
│   │   ├── TripOverview.tsx    # 旅行总览
│   │   ├── TripStatus.tsx      # 行程状态页面
│   │   └── DayDetail.tsx       # 日期详情
│   ├── data/            # 数据文件
│   │   └── mexico.ts           # 墨西哥行程数据
│   ├── types/           # TypeScript 类型定义
│   │   └── index.ts
│   ├── utils/           # 工具函数
│   │   └── dateUtils.ts        # 日期处理逻辑
│   ├── App.tsx          # 主应用组件
│   └── main.tsx         # 入口文件
├── package.json
├── vite.config.ts       # Vite 配置
└── tsconfig.json        # TypeScript 配置
```

## ✨ 核心功能

### 1. 智能日期检测
- **响应式日期状态**：自动检测行程状态（即将开始/进行中/已结束）
- **智能状态页面**：根据当前日期显示不同状态页面
  - 如果今天在行程之前 → 显示"旅行尚未开始"页面
  - 如果今天在行程期间 → 显示"旅行进行中"并跳转到对应日期
  - 如果今天在行程之后 → 显示"旅行已结束"页面
- **实时状态更新**：根据当前日期动态显示信息

### 2. 可扩展架构
- **类型安全**：完整的 TypeScript 类型定义
- **组件化设计**：可复用的 React 组件
- **数据驱动**：行程数据与 UI 分离，易于维护
- **易于扩展**：添加新旅行计划只需添加数据文件

### 3. 现代化 UI
- **响应式设计**：完美支持移动端和桌面端
- **流畅动画**：使用 Tailwind CSS 实现过渡效果
- **深色主题**：护眼的渐变背景设计

## 🚀 本地开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

### 构建生产版本

```bash
npm run build
```

构建产物在 `dist/` 目录

### 预览生产构建

```bash
npm run preview
```

## 📝 如何添加新的旅行计划

1. 在 `src/data/` 目录下创建新的数据文件（例如：`japan.ts`）
2. 参考 `src/data/mexico.ts` 的数据结构
3. 在 `src/pages/Home.tsx` 中导入并添加到 `trips` 数组
4. 在 `src/pages/TripOverview.tsx` 中注册新的 trip

示例：

```typescript
// src/data/japan.ts
export const japanTrip: Trip = {
  id: 'japan',
  name: '日本之旅',
  // ... 其他数据
};

// src/pages/Home.tsx
import { japanTrip } from '../data/japan';
const trips = [mexicoTrip, japanTrip];
```

## 🔧 部署

项目使用 GitHub Actions 自动部署到 GitHub Pages：

1. 推送到 `main` 分支
2. GitHub Actions 自动构建
3. 部署到 `gh-pages` 分支

## 📦 GitHub Pages 托管

### ✅ 完全支持复杂网站

GitHub Pages **完全可以**托管你的 React + TypeScript 网站，无论多复杂！

**原因**：
- React 构建后就是静态文件（HTML、CSS、JS）
- React Router 是客户端路由，在浏览器中处理
- 所有数据都在前端，无需后端服务器
- GitHub Actions 自动构建和部署

### 📈 可以继续扩展

只要保持静态构建、客户端路由、无后端依赖，就可以一直使用 GitHub Pages 免费托管。

可以添加：
- ✅ 更多旅行计划
- ✅ 更多页面和功能
- ✅ 状态管理（Redux、Zustand）
- ✅ PWA 功能
- ✅ 国际化（i18n）

不能添加（需要后端）：
- ❌ 用户认证（但可用 Firebase 等）
- ❌ 数据库（但可用 Firebase、Supabase 等）
- ❌ 服务器端 API（但可用 Serverless Functions）

## 📄 License

MIT
